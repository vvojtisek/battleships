{
  "name": "@bs/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@bs/engine": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  }
}
