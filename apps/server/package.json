{
  "name": "@bs/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@bs/engine": "workspace:*",
    "@bs/protocol": "workspace:*",
    "@fastify/websocket": "^11.2.0",
    "fastify": "^5.6.0"
  },
  "devDependencies": {
    "ws": "^8.18.3"
  },
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/main.js",
    "typecheck": "tsc -b --pretty false",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  }
}
